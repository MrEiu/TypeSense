/**
 * src/schema/questionnaire-schema-types.ts
 *
 * Backward-compatible facade re-exporting the authoritative questionnaire schema.
 * All core schema models, normalizers, and validators are located in src/schema/.
 */

export * from './index';
