# TypeSense 多 Agent 并发问卷生成业务与执行逻辑规范

本文档定义基于 **Map-Reduce 范式（任务拆解 - 并发出题 - 缓存解耦 - 顺序串联）** 的多 Agent 问卷生成系统执行逻辑与流转规范。

---

## 1. 架构总览与拓扑流转

系统将问卷生成拆解为“任务拆解”、“并发生成”与“顺序装配”三个分层阶段，不引入额外的 AI 跨块连线，依靠纯代码确定性串联。

```
                    ┌────────────────────────┐
                    │ 用户需求 + 逻辑模板     │
                    └───────────┬────────────┘
                                │
                                ▼
                    ┌────────────────────────┐
                    │ 1. Dispatcher Agent    │ ── 直接输出各组块的执行提示词（Sub-Prompts）
                    └───────────┬────────────┘
                                │
                                ▼ （分发至并发池）
         ┌──────────────────────┼──────────────────────┐
         ▼                      ▼                      ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ Worker Agent b1 │    │ Worker Agent b2 │    │ Worker Agent b3 │
│  生成 [b1_1...] │    │  生成 [b2_1...] │    │  生成 [b3_1...] │
└────────┬────────┘    └────────┬────────┘    └────────┬────────┘
         │                      │                      │
         └──────────────────────┼──────────────────────┘
                                ▼ （独立写入）
                    ┌────────────────────────┐
                    │ 2. 组块级独立缓存机制    │ ── 故障隔离，支持单块重试与前端流式响应
                    └───────────┬────────────┘
                                │
                                ▼ （所有组块就绪）
                    ┌────────────────────────┐
                    │ 3. 顺序串联与题号重排   │ ── 纯代码：按规划顺序拼接题目，映射为 q1..qN，直接落盘
                    └────────────────────────┘
```

---

## 2. 阶段一：系统统筹与任务拆解（Dispatcher Agent）

### 2.1 职责定义
负责将全局调研诉求拆解为 4~6 个独立的子任务。不输出任何模糊的描述性修饰词，直接输出**发给各子 Agent 的具体出题指令（Sub-Prompts）**。

### 2.2 执行逻辑
1. **直接产出子提示词**：
   Dispatcher 针对每个组块直接输出带有明确约束的提示词指令（包含问什么、禁止问什么），后端无须做二次模板拼接。
2. **输出契约**：
   产出标准任务计划 `SurveyTaskPlan`，包含全局标题及各子任务的 `id`、`count`、`prompt`。

---

## 3. 阶段二：组块并发出题与缓存解耦（Worker Agents）

### 3.1 独立上下文与命名空间题号
每个 Worker Agent 直接接收对应的 `task.prompt` 并发运行：
- **节点命名空间**：Worker 必须为题目赋予带有组块标识的节点 ID：
  - 组块 `b1` 生成：`b1_1, b1_2, b1_3...`
  - 组块 `b2` 生成：`b2_1, b2_2, b2_3...`
- **拓扑边界限制**：Worker 仅允许配置本组块内的局部跳转（例如 `to: "b1_3"`）或终态（`exit` / `end`），严禁跨块跳转。

### 3.2 独立缓存落盘机制（Cache-as-a-Buffer）
- **存储结构**：`cache[surveyId][blockId] = questions`。
- **工程优势**：
  1. **局部容错与重试**：单组块生成失败或超时时，仅需单独重试该 Worker，无需推翻全量任务；
  2. **流式渲染**：前端可按组块完成进度实时上屏展示；
  3. **局部微调**：支持用户单独针对某个组块注入微调指令并重绘。
- **并发控制**：采用并发池（限制最大 3 个并行 Worker），防止触发 API 限流。

---

## 4. 阶段三：顺序串联与题号重排（Sequential Assembly & Re-indexing）

所有组块完成并入缓存后，由**后端确定性代码（纯 TypeScript）**直接按顺序拼装，不交由 AI 做多余操作。

### 4.1 执行逻辑
1. **按序铺平题目**：按任务规划顺序依次读取缓存 `b1 -> b2 -> b3 ...`，平铺为一个完整题目数组。
2. **全局重排为 q1..qN**：
   - 建立映射字典：`{ "b1_1": "q1", "b1_2": "q2", "b2_1": "q3", ... }`；
   - 更新题目自身 `id` 为 `qK`；
   - 若题目含有块内局部跳转 `when: { "b1_1": 0 }, to: "b1_3"`，按字典平移为 `when: { "q1": 0 }, to: "q3"`。
3. **组块间自然衔接**：前一组块的最后一题直接自然顺延至下一组块的第一题，无需配置跨块跳转。

