# TypeSense 多 Agent 并发问卷生成业务与执行逻辑规范

本文档定义基于 **Map-Reduce 范式（统筹拆解 - 并发出题 - 缓存解耦 - 拓扑重排 - AI 连线）** 的多 Agent 问卷生成系统执行逻辑与流转规范。

---

## 1. 架构总览与拓扑流转

系统将问卷生成拆解为“系统统筹”、“并发生成”、“确定性装配”与“逻辑连线”四个分层阶段。

```
                    ┌────────────────────────┐
                    │ 用户需求 + 逻辑模板     │
                    └───────────┬────────────┘
                                │
                                ▼
                    ┌────────────────────────┐
                    │ 1. Dispatcher Agent    │ ── 规划蓝图，划定各组块 In-Scope / Out-of-Scope 边界
                    └───────────┬────────────┘
                                │
                                ▼ （分发至并发池）
         ┌──────────────────────┼──────────────────────┐
         ▼                      ▼                      ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ Worker Agent b1 │    │ Worker Agent b2 │    │ Worker Agent b3 │
│  生成 [b1_1...] │    │  生成 [b2_1...] │    │  生成 [b3_1...] │
└────────┬────────┘    └────────┬────────┘    └────────┬────────┘
         │                      │                      │
         └──────────────────────┼──────────────────────┘
                                ▼ （独立写入）
                    ┌────────────────────────┐
                    │ 2. 组块级独立缓存机制    │ ── 故障隔离，支持单块重试与前端流式响应
                    └───────────┬────────────┘
                                │
                                ▼ （所有组块就绪）
                    ┌────────────────────────┐
                    │ 3. 系统确定性重排与平移 │ ── 纯代码：构建全局 ID 映射表，平移块内局部边
                    └───────────┬────────────┘
                                │ （产出全局连续节点 q1..qN）
                                ▼
                    ┌────────────────────────┐
                    │ 4. Linker AI 拓扑连线  │ ── 仅依据模板注入跨组块 Jump，不修改题目内容
                    └───────────┬────────────┘
                                │
                                ▼
                    ┌────────────────────────┐
                    │ 5. FastAcyclicGuard    │ ── DFS 三态染色算法切断环路，确保单向收敛落盘
                    └────────────────────────┘
```

---

## 2. 阶段一：系统统筹与语义隔离规划（Dispatcher Agent）

### 2.1 职责定义
负责将用户的全局调研诉求拆解为 4~6 个业务组块，并为每个组块划定**内容约束边界**，杜绝并发生成时的撞题与内容重复。

### 2.2 执行逻辑
1. **显式内容边界生成（Scope Definition）**：
   - Dispatcher 必须为每个组块明确输出：
     - `inScope`: 当前组块的核心考察维度（必须涵盖）；
     - `outOfScope`: 严禁涉及的维度（交由其他组块负责）。
2. **输出落盘**：产出结构化蓝图 `SurveyBlueprint`，驱动后续 Worker 派发。

---

## 3. 阶段二：组块并发出题与缓存解耦（Worker Agents）

### 3.1 独立上下文与命名空间题号
每个 Worker Agent 独立运行，互不感知其他 Agent 的生成过程：
- **节点命名空间**：Worker 必须为题目赋予带有组块标识的节点 ID：
  - 组块 `b1` 生成：`b1_1, b1_2, b1_3...`
  - 组块 `b2` 生成：`b2_1, b2_2, b2_3...`
- **拓扑边界限制**：Worker 仅允许配置本组块内的局部跳转（例如 `to: "b1_3"`）或终态（`exit` / `end`），**严禁跨块跳转**。

### 3.2 独立缓存落盘机制（Cache-as-a-Buffer）
- **存储结构**：`cache[surveyId][blockId] = questions`。
- **工程优势**：
  1. **局部容错与重试**：单组块生成失败或超时时，仅需单独重试该 Worker，无需推翻全量任务；
  2. **流式渲染友好**：前端可基于 Server-Sent Events（SSE）按组块完成进度实时上屏展示；
  3. **单块人工干预**：支持用户单独针对某个组块注入微调指令并局部重绘。
- **并发控制**：采用并发池（限制最大 3 个并行 Worker），防止瞬间打满 LLM API 的 RPM/TPM 限流。

---

## 4. 阶段三：拓扑节点系统重排与边平移（System Re-indexing）

所有组块完成并入缓存后，由**后端确定性代码（纯 TypeScript）**执行机械组装与图节点映射，杜绝模型幻觉。

### 4.1 节点平移算法
1. **按蓝图顺序铺平节点**：顺序读取 `b1 -> b2 -> b3 ...` 的题目列表。
2. **构建映射字典（Mapping Dictionary）**：
   ```typescript
   const idMap: Record<string, string> = {
     "b1_1": "q1",
     "b1_2": "q2",
     "b2_1": "q3",
     "b2_2": "q4",
     // ...
   };
   ```
3. **同步更新题目 ID 与块内跳转（Edge Translation）**：
   - 更新题目自身 `q.id = idMap[q.id]`；
   - 遍历每道题目的 `jump` 规则：
     - 若 `rule.to` 存在于 `idMap` 中，自动替换为对应的全局题号（如 `to: "b1_2"` 转换为 `to: "q2"`）；
     - 若条件内含有前序题号引用 `when: { "b1_1": 0 }`，同步平移为 `when: { "q1": 0 }`。

---

## 5. 阶段四：系统 AI 跨组块拓扑连线（Linker AI）

### 5.1 职责定位
Linker AI 的定位是“**拓扑连线员**”，负责根据选定的逻辑模板在各组块之间建立跳转网络。

### 5.2 严格执行纪律
1. **严禁重写题干与选项**：保持 Worker Agent 生成的题干、选项原样不变，防止二次幻觉；
2. **专注跨组块路由**：
   - 依据逻辑模板（如“前置筛选”），识别分水岭题目（如 `q1` 选 1 表示从未使用）；
   - 在该题目上注入跨组块跳转规则：`{ "when": { "q1": 1 }, "to": "q6" }` 或 `{ "to": "exit" }`；
3. **输出形式**：输出带有完整 `jump` 连线配置的题目数组。

---

## 6. 阶段五：系统级 DAG 安全守卫（Fast Acyclic Guard）

Linker AI 输出后，由后端代码执行 $O(V+E)$ 拓扑安全校验：

### 6.1 悬空目标与自环清理
- 收集所有有效节点 `existingIds = new Set(["q1", "q2", ...])`；
- 移除指向自身（`to === currentId`）的自环；
- 移除指向不存在题号的悬空跳转（保留 `exit` 与 `end` 终态）。

### 6.2 DFS 三态染色环路切断
- 0 = 未访问，1 = 访问中（当前调用栈），2 = 已完成；
- 遍历节点出边，若目标节点状态为 1，判定为回跳环路，**立即切断该跳转边**并降级为自然推进；
- 确保最终交付前端的问卷拓扑为一个**严格单向有向无环图（DAG）**。

---

## 7. 异常容错与重试矩阵

| 场景 | 处理策略 |
| :--- | :--- |
| **单个 Worker Agent 失败 / 超时** | 触发单组块重试（最多 2 次），其余并发 Worker 正常执行不受影响。 |
| **Worker 题数不符合分配要求** | 后端代码按 `questionCount` 截断或在组装期做标号补齐，不阻塞整体流程。 |
| **Linker AI 未生成任何跨块跳转** | 判定为安全降级，问卷保持线性自然递进流转。 |
| **Linker AI 引入回跳死循环** | `FastAcyclicGuard` 毫秒级自动切断环路，平滑落盘。 |
